# LinkedIn Draft

PatchGym is a local-first coding-agent benchmark harness.

It turns real Git history into verifiable tasks: base commit, hidden tests,
oracle patch, agent run, and report. The point is not to replace public
benchmarks. It is to help teams ask whether an agent can fix their own codebase
under their own tests.

The implementation is intentionally small, readable, and source-installable.
