# Limitations

- Works most reliably when tests and fixes land in the same commit.
- Uses path heuristics for test detection.
- Does not provide strong sandboxing.
- Does not claim a complete public-benchmark export format.
- Does not publish or host datasets.
- Does not include a public leaderboard.

These constraints keep the MVP small, local, and inspectable.
